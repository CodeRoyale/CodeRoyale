//- Please fill these

const encryptData = (data) => {
  // encrypt data
  // use symmetric keys
  // use block encryption
  // on way encrption is fine
  return data;
};

module.exports = {
  encryptData,
};
