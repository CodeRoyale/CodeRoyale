const ROOM_DEFAULTS = {
  maxTeams: 5,
  maxPerTeam: 5,
  maxPerRoom: 25,
};

const ROOM_LIMITS = {
  maxTeams: 25,
  maxPerTeam: 10,
  maxPerRoom: 250,
};

module.exports = { ROOM_DEFAULTS, ROOM_LIMITS };
