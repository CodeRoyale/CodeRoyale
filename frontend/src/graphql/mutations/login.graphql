mutation Login($email: String!) {
  login(email: $email) {
    errors {
      field
      message
    }
    user {
      id
      username
      name
      email
      profilePicture
      bio
    }
  }
}
